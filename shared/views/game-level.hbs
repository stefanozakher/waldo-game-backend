{{#if (eq gameSession.status 'playing')}}
    {{#if currentLevel}}
    <img id="game-board-image" src="{{currentLevel.imageUrl}}" alt="{{currentLevel.title}}" data-level-id="{{currentLevelId}}" />
    <!-- https://worka.github.io/files/wheel_zoom_example.jpg -->
    {{/if}}
{{/if}}
